# SICAD

#Executar projeto frontend mvn clean package & mvn javafx:run
#Executar projeto backend só executar pelo vs mesmo

Instalar Maven descompactar a pasta Apache e colocar em arquivos de programas

depois vai em variaveis de ambiente do windows

e em variaveis do sistema, vc adicionar isso

Nome da variavel: MAVEN_HOME
Valor da variavel: C:\Program Files\Apache\Maven

depois vc da 2 clicks em Path dentro de variaveis do sistema

clica em novo e adicionar isso

%MAVEN_HOME%\bin